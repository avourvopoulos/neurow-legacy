using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class ShowLevelName : MonoBehaviour {

	// Use this for initialization
	void Start()
	{
//		gameObject.GetComponent ("Text").guiText.text = Application.loadedLevelName;
	}
	
	// Update is called once per frame
	void Update()
	{
	//	gameObject.GetComponent<Text>().text = Application.loadedLevelName;
	}
}
